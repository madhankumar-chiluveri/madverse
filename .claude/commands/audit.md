Your goal is to update vulnerable dependencies.

Do the following:
1. Run the `npm audit` command.
2. If there are any vulnerabilities, run the `npm audit fix` command.
3. If there are any breaking changes, run the `npm audit fix --force` command.
4. Run the `npm audit` command again to verify that there are no vulnerabilities.